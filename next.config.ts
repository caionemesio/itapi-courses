import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  images: {
    domains: ['www.google.com', 'www.alura.com.br'], // Adicione outros domínios que você precisar
  },
}

export default nextConfig
